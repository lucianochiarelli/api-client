apiVersion: v1
kind: Service
metadata:
  name: {{ template "api-client.fullname" . }}
  labels:
    app: {{ template "api-client.name" . }}
    chart: {{ template "api-client.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "api-client.name" . }}
    release: {{ .Release.Name }}
